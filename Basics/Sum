\\ Write a C programme to find the sum of natural numbers
 #include<stdio.h>
  int main() {
 \\ read the in put by the user
  int n;
  printf("input the n number : ");
  scanf("%d", &n);
  int sum=(n*(n+1)/2);
  printf("%d",sum);
  return 0;
